<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.campus.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.campus.model.Category">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
    </resultMap>

    <insert id="insertCategory" parameterType="com.campus.model.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_category (name)
        VALUES (#{name,jdbcType=VARCHAR})
    </insert>

    <select id="selectAllCategories" resultMap="BaseResultMap">
        SELECT
            id, name
        FROM t_category
    </select>

</mapper>