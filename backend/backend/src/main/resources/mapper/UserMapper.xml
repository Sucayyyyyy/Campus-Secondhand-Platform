<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.UserMapper"> <resultMap id="UserResultMap" type="com.campus.model.User">
    <id column="id" property="id"/>
    <result column="username" property="username"/>
    <result column="password" property="password"/>
    <result column="phone" property="phone"/>
    <result column="create_time" property="createTime"/>
</resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, phone, create_time
        FROM t_user
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.campus.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, phone, create_time)
        VALUES (#{username}, #{password}, #{phone}, NOW()) </insert>

    <sql id="User_Base_Column_List">
        id, username, email, phone, avatar_url, register_time
    </sql>

    <select id="selectById" parameterType="int" resultType="com.campus.model.User">
        SELECT
        <include refid="User_Base_Column_List" />
        FROM t_user
        WHERE id = #{id}
    </select>

</mapper>